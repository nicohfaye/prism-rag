# Copy to docker/.env and fill in. Values here are overrides for prism-rag settings.
# Nested fields use double-underscore: PRISM_<SECTION>__<FIELD>.

# OpenAI (used when profile=openai)
OPENAI_API_KEY=

# Override config fields via env (optional):
# PRISM_EMBEDDING__PROVIDER=ollama
# PRISM_EMBEDDING__MODEL=nomic-embed-text
# PRISM_GENERATION__PROVIDER=ollama
# PRISM_GENERATION__MODEL=gpt-oss:20b
# PRISM_MILVUS__URI=http://localhost:19530
